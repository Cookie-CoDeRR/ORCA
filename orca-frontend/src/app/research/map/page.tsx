export default function SpatialCanvasPage() {
  return (
    <div className="w-full h-full bg-slate-950 flex items-center justify-center">
      {/* deck.gl map will mount here. No other UI elements allowed to preserve canvas space. */}
      <span className="text-slate-600 font-mono">deck.gl spatial canvas placeholder</span>
    </div>
  );
}
